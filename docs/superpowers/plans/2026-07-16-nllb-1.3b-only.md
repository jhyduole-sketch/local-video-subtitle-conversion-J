# NLLB 1.3B 统一模型实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 移除 NLLB 600M 的用户入口和运行时使用，统一使用 NLLB 1.3B，同时兼容旧 CLI 参数。

**Architecture:** 保留现有翻译器接口与 `local-nllb` 旧参数，但将它映射到 1.3B；Web 只展示一个本地多语言模型，健康检查只检查 1.3B，z.ai 自动回退也直接使用 1.3B。已有 600M 缓存不主动删除。

**Tech Stack:** Python 3、`unittest`、Transformers、原生 HTML/JavaScript。

## Global Constraints

- 不自动删除用户已经下载的 600M 模型文件。
- 不破坏旧 CLI 命令中的 `--translator local-nllb`。
- 本轮不提交或推送 GitHub，完成后等待用户确认。

---

### Task 1: 统一翻译路由

**Files:**
- Modify: `tests/test_pipeline.py`
- Modify: `src/subtitle_tool/pipeline.py`

- [ ] 先写测试，要求 `local-nllb`、`local-nllb-quality` 和 z.ai 的 NLLB 回退均调用 `facebook/nllb-200-distilled-1.3B`。
- [ ] 运行定向测试，确认旧实现因仍调用 600M 而失败。
- [ ] 修改路由并运行测试，确认兼容参数和自动回退全部通过。

### Task 2: 精简 Web、CLI 与环境检查

**Files:**
- Modify: `tests/test_web.py`
- Modify: `tests/test_cli.py`
- Modify: `src/subtitle_tool/web.py`
- Modify: `src/subtitle_tool/cli.py`
- Modify: `src/subtitle_tool/web_assets/index.html`
- Modify: `src/subtitle_tool/web_assets/app.js`

- [ ] 先写测试，要求健康检查只列出 NLLB 1.3B。
- [ ] 删除 Web 中的 600M 选项，将旧任务中的 `local-nllb` 显示和语言选择映射到 1.3B。
- [ ] 更新 CLI 帮助，明确两个兼容参数都使用 1.3B。

### Task 3: 文档与完整验证

**Files:**
- Modify: `README.md`
- Modify: `docs/使用指南.md`
- Modify: `docs/更新记录.md`

- [ ] 删除 600M 推荐、下载命令和双模型说明，改成统一的 NLLB 1.3B。
- [ ] 运行全量单元测试、Python 编译、JavaScript 语法检查和 `git diff --check`。
- [ ] 重启 `0.0.0.0:7860`，检查 `/api/health`，等待用户测试确认。
